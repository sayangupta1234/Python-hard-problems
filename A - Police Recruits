n = int(input())
arr = list(map(int, input().split()))

officers = 0
untreated = 0

for a in arr:
    if a == -1:
        if officers > 0:
            officers -= 1
        else:
            untreated += 1
    else:
        officers += a

print(untreated)
